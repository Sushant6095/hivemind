{
  // Compiler pass. Invoked explicitly from telegram-webhook after each RawMessage
  // (this app runs the Workflows runtime, so legacy entity automations are off).
  // The 5-min burst_sweeper lives as a scheduled Workflow calling this with {"sweep": true}.
  "name": "process-messages",
  "entry": "entry.ts"
}
