{
  // Daily 09:00 IST (cron "30 3 * * *"). Runs as a scheduled Workflow that
  // invokes this function (legacy automation removed — Workflows runtime).
  "name": "nudge-commitments",
  "entry": "entry.ts"
}
