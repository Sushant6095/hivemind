{
  // Sunday 18:00 IST (cron "30 12 * * 0") as a scheduled Workflow; also on-demand
  // via /digest in telegram-webhook (legacy automation removed — Workflows runtime).
  "name": "weekly-digest",
  "entry": "entry.ts"
}
