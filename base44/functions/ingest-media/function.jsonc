{
  // Media/receipt pipeline. Invoked explicitly from telegram-webhook with
  // { data: <created RawMessage> } for media messages (Workflows runtime — no
  // legacy entity automation).
  "name": "ingest-media",
  "entry": "entry.ts"
}
