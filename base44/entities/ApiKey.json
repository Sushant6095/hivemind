{
  "name": "ApiKey",
  "type": "object",
  "properties": {
    "space_id": { "type": "string" },
    "key": { "type": "string", "description": "Secret bearer key for the headless read API (hmk_…). Generated/revoked owner-only via the `api` function." },
    "label": { "type": "string", "description": "Human label so owners can tell keys apart" },
    "member_emails": { "type": "array", "items": { "type": "string" }, "description": "RLS mirror of the parent space's member list" }
  },
  "required": ["space_id", "key"],
  "rls": {
    "read": { "member_emails": "{{user.email}}" },
    "create": false,
    "update": false,
    "delete": false
  }
}
