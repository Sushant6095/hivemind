# Demo GIFs — shot list

Drop the recorded files here with these exact names so the README picks them up.
Record at ~1000–1200px wide, 8–15 fps, keep each ≤ ~6 MB (loop-friendly).

| File | Shot | How to capture |
|---|---|---|
| `compile.gif` | **Hero** — split screen: Telegram (left) receives *“ok final: Goa Aug 14, budget ₹40k, Priya books flights by Friday”* while the dashboard **Board/Live feed** (right) grows a Decision + Event + Commitment. | Two windows side by side; send the message; record ~6 s. |
| `sidekick.gif` | Chrome **Sidekick** side panel opening beside any tab, showing the live feed (`?panel=1` chrome-less mode). | Load `extension/` unpacked → click the 🐝 action → panel slides in. |
| `import.gif` | **Import** tab: pick a Telegram `result.json` → progress bar fills `done/total` → cards land on the board. | Use a real export (or `seed-demo`); record the progress bar to completion. |
| `engine-room.gif` | Owner-only **Engine Room**: terminal tail of app-logs auto-refreshing + counter row. | Log in as a space owner → Engine Room tab → trigger a compile in another window. |

Tip: `compile.gif` is the money shot — it proves the “message → live card” claim. Capture it first.
